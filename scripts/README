generate_tree.py generates complete d-nary tree with height h. 
run with:
python generate_tree.py h d > tree_h_d.gr

generate_pebbling.py generates a pebbling-like graph with height h.
run with:
python generate_pebbling.py h > pebbling_h_d.gr

runMerge.sh queues jobs for the mergeability calculations of a folder of instances
run with:
runMerge.sh <DIRECTORY_OF_INSTANCES> <COMPILED_MERGE_CALCULATOR>

getMergeData.py generates a file containing mergeability stats for the runMerge.sh outputs
run with:
python getMergeData.py <DIRECTORY_THAT_RUNMERGE_USED>
